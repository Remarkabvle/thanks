export class Car {
    constructor(name, year, distance, color, brand ) {
      this._name = name;
      this._year = year;
      this._distance = distance;
      this._color = color;
      this._brand = brand;
    }
    setName(newName) {
        this._name = newName;
        return `"Moshinani nomi o'zgartirildi"`;
      }

    getName() {
        return `Moshinani nomi -- ${this._name}`;
      }

    setYear(newYear) {
        this._year = newYear;
        return `"year o'zgartirildi"`;
      }

    getYear() {
        return `yearning nomi -- ${this._year}`;
      }

    setDistance(newDistance) {
        this._distance = newDistance;
        return `"distance o'zgartirildi"`;
      }

    getDistance() {
        return `distancening nomi -- ${this._distance}`;
      }

    setColor(newColor) {
        this._color = newColor;
        return `"color o'zgartirildi"`;
      }

    getColor() {
        return `colorning nomi -- ${this._color}`;
      }

    setBrand(newBrand) {
        this._brand = newBrand;
        return `"brand o'zgartirildi"`;
      }

    getBrand() {
        return `brandning nomi -- ${this._brand}`;
      }
}

export class Laptop {
    constructor(name, year, processor, color, memory ) {
      this._name = name;
      this._year = year;
      this._processor = processor;
      this._color = color;
      this._memory = memory;
    }
    setName(newName) {
        this._name = newName;
        return `"Laptop nomi o'zgartirildi"`;
      }

    getName() {
        return `Laptop nomi -- ${this._name}`;
      }

    setYear(newYear) {
        this._year = newYear;
        return `"year o'zgartirildi"`;
      }

    getYear() {
        return `yearning nomi -- ${this._year}`;
      }

    setProcessor(newProcessor) {
        this._processor= newProcessor;
        return `"Processor o'zgartirildi"`;
      }

    getProcessor() {
        return `Processor nomi -- ${this._processor}`;
      }

    setColor(newColor) {
        this._color = newColor;
        return `"color o'zgartirildi"`;
      }

    getColor() {
        return `colorning nomi -- ${this._color}`;
      }

    setMemory(newMemory) {
        this._memory = newMemory;
        return `"Memory o'zgartirildi"`;
      }

    getMemory() {
        return `Memory nomi -- ${this._memory}`;
      }
}

export class Phone {
    constructor(name, year, type, color, memory ) {
      this._name = name;
      this._year = year;
      this._type = type;
      this._color = color;
      this._memory = memory;
    }
    setName(newName) {
        this._name = newName;
        return `"Phone nomi o'zgartirildi"`;
      }

    getName() {
        return `Phone nomi -- ${this._name}`;
      }

    setYear(newYear) {
        this._year = newYear;
        return `"year o'zgartirildi"`;
      }

    getYear() {
        return `yearning nomi -- ${this._year}`;
      }

    setType(newType) {
        this._type= newType;
        return `"Type o'zgartirildi"`;
      }

    getType() {
        return `Type nomi -- ${this._type}`;
      }

    setColor(newColor) {
        this._color = newColor;
        return `"color o'zgartirildi"`;
      }

    getColor() {
        return `colorning nomi -- ${this._color}`;
      }

    setMemory(newMemory) {
        this._memory = newMemory;
        return `"Memory o'zgartirildi"`;
      }

    getMemory() {
        return `Memory nomi -- ${this._memory}`;
      }
}

export class Watch {
    constructor(name, year, type, color, brand ) {
      this._name = name;
      this._year = year;
      this._type = type;
      this._color = color;
      this._brand = brand;
    }
    setName(newName) {
        this._name = newName;
        return `"Watch nomi o'zgartirildi"`;
      }

    getName() {
        return `Watch nomi -- ${this._name}`;
      }

    setYear(newYear) {
        this._year = newYear;
        return `"year o'zgartirildi"`;
      }

    getYear() {
        return `yearning nomi -- ${this._year}`;
      }

    setType(newType) {
        this._type = newType;
        return `"Type o'zgartirildi"`;
      }

    getType() {
        return `Type nomi -- ${this._Type}`;
      }

    setColor(newColor) {
        this._color = newColor;
        return `"color o'zgartirildi"`;
      }

    getColor() {
        return `colorning nomi -- ${this._color}`;
      }

    setBrand(newBrand) {
        this._brand = newBrand;
        return `"brand o'zgartirildi"`;
      }

    getBrand() {
        return `brandning nomi -- ${this._brand}`;
      }
}

export class Mouse {
    constructor(name, year, type, color, brand ) {
      this._name = name;
      this._year = year;
      this._type = type;
      this._color = color;
      this._brand = brand;
    }
    setName(newName) {
        this._name = newName;
        return `"Mouse nomi o'zgartirildi"`;
      }

    getName() {
        return `Mouse nomi -- ${this._name}`;
      }

    setYear(newYear) {
        this._year = newYear;
        return `"year o'zgartirildi"`;
      }

    getYear() {
        return `yearning nomi -- ${this._year}`;
      }

    setType(newType) {
        this._type = newType;
        return `"Type o'zgartirildi"`;
      }

    getType() {
        return `Type nomi -- ${this._Type}`;
      }

    setColor(newColor) {
        this._color = newColor;
        return `"color o'zgartirildi"`;
      }

    getColor() {
        return `colorning nomi -- ${this._color}`;
      }

    setBrand(newBrand) {
        this._brand = newBrand;
        return `"brand o'zgartirildi"`;
      }

    getBrand() {
        return `brandning nomi -- ${this._brand}`;
      }
}

